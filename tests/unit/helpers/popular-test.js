import { popular } from 'questions/helpers/popular';
import { module, test } from 'qunit';

module('Unit | Helper | popular');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = popular([42]);
  assert.ok(result);
});
