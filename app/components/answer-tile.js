import Ember from 'ember';

export default Ember.Component.extend({
});

// totalVotes: Ember.inject.service(),
//
// currentVotes: Ember.computed('totalVotes', function() {
//   return totalVotes.votes.length;
// }),
//
// actions: {
//   addToVotes(vote) {
//     this.get('totalVotes').add(vote);
//   }
// }
